import { ThreeAnchorDirective } from './three-anchor.directive';

describe('ThreeAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
