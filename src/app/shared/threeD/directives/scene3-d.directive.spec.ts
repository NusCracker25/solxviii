import { Scene3DDirective } from './scene3-d.directive';

describe('Scene3DDirective', () => {
  it('should create an instance', () => {
    const directive = new Scene3DDirective();
    expect(directive).toBeTruthy();
  });
});
