import { AddToSceneDirective } from './add-to-scene.directive';

describe('AddToSceneDirective', () => {
  it('should create an instance', () => {
    const directive = new AddToSceneDirective();
    expect(directive).toBeTruthy();
  });
});
